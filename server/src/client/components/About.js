import React from 'react';

const About = () => {
  return (
    <div>
      <div>I'm the About component</div>
      <button onClick={() => console.log('Hi there!')}>Press me!</button>
    </div>
  );
};

export default About;
